function Home() {
    return(
        <section className="page">
            <h2>Bienvenido</h2>
            <p>Este es el frontend con React + Vite. Y el backend con Python corre en una segunda terminal, utilizando FastAPI para crear la API que permite la comunicación entre ambos</p>
        </section>
    );
}
export default Home;